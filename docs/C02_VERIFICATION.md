# Verification scope

Source: work/e_cf_four5_20260925/release plus ecf_study C02. Existing F05 folder and historical experiments are untouched. The C02 objectives module removes the unused local-softmax branch and rejects non-C02 arms; state-dictionary parameter names are preserved. Public factories use the descriptive evidence_supervision namespace.

Local verification receipt is in verification.json. This cleanup does not imply deterministic GPU training or a newly completed end-to-end retraining. Original experimental results belong to the sealed original implementation. Real-data full checkpoint parity for this portable namespace/path change must be checked before using it as a new training or evaluation reference.

README inherited installation/data instructions apply to QV. The optional MR task is an architectural switch, not a complete cross-dataset implementation. Legacy diagnostics and lower-level compatibility names remain deliberately; numerical refactoring of those internals requires separate equivalence checks.
